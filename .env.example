# Application Resolution MCP Server environment configuration

# Required: Base URL for the downstream FastAPI resolution service.
RESOLUTION_SERVICE_URL=http://localhost:8000

# Optional: Timeout (in seconds) for HTTP requests to the FastAPI service.
API_TIMEOUT=30

# Optional: Port for the MCP SSE server transport.
MCP_SSE_PORT=8000
